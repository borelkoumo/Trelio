Always use Context7 when I need library/API documentation, code generation, setup or configuration steps without me having to explicitly ask.

Always update the documentation is necessary, so that it's always aligned with codebase.

Whenever a change on code impacts database, update the supabase/schema.sql file